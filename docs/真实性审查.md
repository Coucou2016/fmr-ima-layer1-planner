# 真实性审查（简版）

完整英文/中英对照证据链见 [`docs/evidence_audit.md`](evidence_audit.md)。

**结论要点：**

1. 论文与报告数字对齐当前仓库 `python run_pipeline.py --seed 42 --paper` 输出，不以 Galili 表值冒充本代理预测。
2. tip `8a882f7` 起刷新；默认排序 `response_path=fitted_response`，最优候选 **IMA-CS 20%**（AP↓11%，leakage_proxy≈0.391%）。
3. 折内 CV 与规则基 blend-off、校准复现、情景排序四类指标分源报告，禁止混引。
4. `leakage_proxy` ≠ 临床反流容积；无患者级外部验证声称；MAVERIC=ARTO；DOI 10.1098/rsos.211464。
